---
title: "Consulta uma Busca"
openapi: "GET /requests/{request_id}"
api: "GET https://requests.stage.judit.io/requests/{request_id}"
description: "Esse endpoint consulta uma busca pelo id"
---

<ParamField path="request_id" type="string" required>
    O ID da busca que você deseja consultar.
</ParamField>

<RequestExample>
    ```bash Curl
    curl -X GET "https://requests.stage.judit.io/requests/71adfaa6-5485-45ce-a4a1-00d485f8f0f9"
    -H "api-key: YOUR_API_KEY"
    ```

    ```request Node
    const options = {
        method: 'GET',
        headers: {api-key: 'c194e4c9-af6c-482f-b695-a1267c7d1ee7'},
    };

    fetch("https://requests.stage.judit.io/requests", options)
    .then(response => response.json())
    .then(response => console.log(response))
    .catch(err => console.error(err));
    ```

    ```request PHP
    $ch   = curl_init();

    curl_setopt($ch, CURLOPT_URL, 'https://requests.stage.judit.io/requests/71adfaa6-5485-45ce-a4a1-00d485f8f0f9');
    curl_setopt($ch, CURLOPT_GET, 1);

s    curl_close($ch);
    $result =

    $response = curl_exec($ch);
    $err = curl_error($ch);

    curl_close($ch);

    if ($err) {
        echo "cURL Error #:" . $err;
    } else {
        echo $response;
    }
    ```

    ```request Go

    package main

    import (
    "fmt"
    "io/ioutil"
    "log"
    "net/http"
    )

    func main() {
    const Url = "https://requests.stage.judit.io/requests/71adfaa6-5485-45ce-a4a1-00d485f8f0f9"

    response, err := http.Post(Url, "application/json")

    if err != nil {
        log.Fatal(err.Error())
    }

    defer response.Body.Close()
}
    ```

    ```request Python
    import requests

    url = "https://requests.stage.judit.io/requests/71adfaa6-5485-45ce-a4a1-00d485f8f0f9"

    headers = {"api-key": "c194e4c9-af6c-482f-b695-a1267c7d1ee7"}

    response = requests.request("GET", url, headers=headers)

    print(response.text)
    ```

    ```request Java
    HttpResponse<String> response = Unirest.get("https://requests.stage.judit.io/requests/71adfaa6-5485-45ce-a4a1-00d485f8f0f9")
    .header("api-key", "c194e4c9-af6c-482f-b695-a1267c7d1ee7")
    .asString();
    ```
</RequestExample>

<ResponseExample>
    ```json Response
    {
        "request_id": "71adfaa6-5485-45ce-a4a1-00d485f8f0f9",
        "search": {
            "search_type": "lawsuit_cnj",
            "search_key": "0010137-92.2023.5.03.0077",
            "search_params": {},
        },
        "origin": "api",
        "origin_id": "fde89d98-b2b1-4c58-b0b7-13f4f96bba3d",
        "user_id": "5ccba00e-1563-4418-91a4-c164b9f02411",
        "status": "pending",
        "tags": {},
        "created_at": "2023-10-18T18:36:58.775Z",
        "updated_at": "2023-10-18T18:36:58.775Z"
    }
    ```
</ResponseExample>
