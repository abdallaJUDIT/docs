---
title: "Deletar um Monitoramento"
openapi: "DELETE /tracking/{tracking_id}"
api: "DELETE https://tracking.stage.judit.io/tracking/{tracking_id}"
description: "Esse endpoint deleta um monitoramento pelo id"
---

<ParamField path="tracking_id" type="string" required>
    O ID do monitoramento que você deseja deletar.
</ParamField>

<RequestExample>
    ```bash Curl
    curl -X DELETE "https://tracking.stage.judit.io/tracking/754f02d7-e346-48e2-a23e-4455a11af41e"
    -H "api-key: YOUR_API_KEY"
    ```

    ```request Node
    const options = {
    method: 'GET',
    headers: {api-key: 'c194e4c9-af6c-482f-b695-a1267c7d1ee7'},
};

    fetch("https://requests.stage.judit.io/requests", options)
    .then(response => response.json())
    .then(response => console.log(response))
    .catch(err => console.error(err));
    ```

    ```request PHP
    $ch   = curl_init();

    curl_setopt($ch, CURLOPT_URL, 'https://tracking.stage.judit.io/tracking/754f02d7-e346-48e2-a23e-4455a11af41e');
    curl_setopt($ch, CURLOPT_GET, 1);

    s    curl_close($ch);
    $result =

    $response = curl_exec($ch);
    $err = curl_error($ch);

    curl_close($ch);

    if ($err) {
    echo "cURL Error #:" . $err;
} else {
    echo $response;
}
    ```

    ```request Go

    package main

    import (
    "fmt"
    "io/ioutil"
    "log"
    "net/http"
    )

    func main() {
    const Url = "https://tracking.stage.judit.io/tracking/754f02d7-e346-48e2-a23e-4455a11af41e"

    response, err := http.Post(Url, "application/json")

    if err != nil {
    log.Fatal(err.Error())
}

    defer response.Body.Close()
}
    ```

    ```request Python
    import requests

    url = "https://tracking.stage.judit.io/tracking/754f02d7-e346-48e2-a23e-4455a11af41e"

    headers = {"api-key": "c194e4c9-af6c-482f-b695-a1267c7d1ee7"}

    response = requests.request("GET", url, headers=headers)

    print(response.text)
    ```

    ```request Java
    HttpResponse<String> response = Unirest.get("https://tracking.stage.judit.io/tracking/754f02d7-e346-48e2-a23e-4455a11af41e")
    .header("api-key", "c194e4c9-af6c-482f-b695-a1267c7d1ee7")
    .asString();
    ```
</RequestExample>

<ResponseExample>
    ```json Response
    {
        "tracking_id": "754f02d7-e346-48e2-a23e-4455a11af41e",
        "user_id": "454bbd51-3f8a-4889-b45f-54a78e312257",
        "reference_type": "lawsuit_id",
        "reference_id": "0001234-55.2023.8.26.0100",
        "status": "deleted",
        "recurrence": 1,
        "created_at": "2023-10-17T15:17:05.826Z",
        "updated_at": "2023-10-17T15:17:05.826Z",
        "deleted_at": "2023-10-18T18:40:34.742Z",
    }
    ```
</ResponseExample>
