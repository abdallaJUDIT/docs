---
title: "Criar um monitoramento"
openapi: "POST /tracking"
api: "POST https://tracking.stage.judit.io/tracking"
description: "Esse endpoint cria um monitoramento de processos por documento(CPF, CNPJ, OAB) ou Código CNJ"
---

<ParamField body="reference_type" type="string" required>
    Define o tipo de entidade que você quer monitorar 'cpf', 'cnpj', 'oab' ou 'lawsuit_id' para o número do processo.
</ParamField>

<ParamField body="reference_id" type="string" required>
    Número do processo (Código CNJ), CPF, CNPJ ou OAB que você deseja monitorar.
</ParamField>

<ParamField body="recurrence" type="integer" required>
    Recorrência em dias de atualização dos processos.
</ParamField>

<ParamField body="tags" type="object" required={false}>
    <Expandable title="properties">
        <ParamField body="key" name="tag_name" type="string" required={false}>
            key
        </ParamField>
    </Expandable>
</ParamField>

<RequestExample>
    ```bash Curl
    curl -X POST "https://tracking.stage.judit.io/tracking"
    -H "api-key: YOUR_API_KEY"
    -H "Content-Type: application/json"
    -d '{
        "reference_type": "lawsuit_id",
        "reference_id": "0001234-55.2023.8.26.0100",
        "recurrence": 2,
        "tags": { "category": "any" },
    }'
    ```

    ```request Node
    const options = {
        method: 'GET',
        headers: {api-key: 'c194e4c9-af6c-482f-b695-a1267c7d1ee7'},
        body: {
            "reference_type": "lawsuit_id",
            "reference_id": "0001234-55.2023.8.26.0100",
            "recurrence": 2,
            "tags": { "category": "any" }
        }
    };

    fetch("https://tracking.stage.judit.io/tracking", options)
    .then(response => response.json())
    .then(response => console.log(response))
    .catch(err => console.error(err));
    ```

    ```request PHP
    $data = ['api-key' => 'c194e4c9-af6c-482f-b695-a1267c7d1ee7'];
    $data = [
    'reference_type' => 'lawsuit_id',
    'reference_id' => '0001234-55.2023.8.26.0100',
    'recurrence' => 2,
    'tags' => '"category": "any"'];

    $ch   = curl_init();

    curl_setopt($ch, CURLOPT_URL, "https://tracking.stage.judit.io/tracking");
    curl_setopt($ch, CURLOPT_POST, 1);
    curl_setopt($ch, CURLOPT_POSTFIELDS, $data);


    curl_close($ch);
    $result =

    $response = curl_exec($ch);
    $err = curl_error($ch);

    curl_close($ch);

    if ($err) {
    echo "cURL Error #:" . $err;
} else {
    echo $response;
}
    ```

    ```request Go

    package main

    import (
    "fmt"
    "io/ioutil"
    "log"
    "net/http"
    )

    func main() {
    const Url = "https://tracking.stage.judit.io/tracking"

    jsonValue := []byte(`{
            "reference_type": "lawsuit_id",
            "reference_id": "0001234-55.2023.8.26.0100",
            "recurrence": 2,
            "tags": { "category": "any" }
         }`)

    response, err := http.Post(Url, "application/json", bytes.NewBuffer(jsonValue))

    if err != nil {
    log.Fatal(err.Error())
}

    defer response.Body.Close()
}
    ```

    ```request Python
    import requests

    url = "https://tracking.stage.judit.io/tracking"


    new_data = {
        "reference_type": "lawsuit_id",
        "reference_id": "0001234-55.2023.8.26.0100",
        "recurrence": 2,
        "tags": { "category": "any" }
    }

    headers = {"api-key": "c194e4c9-af6c-482f-b695-a1267c7d1ee7"}

    response = tracking.request("POST", url, headers=headers, json=new_data)

    print(response.text)
    ```

    ```request Java
    HttpResponse<String> response = Unirest.post("https://tracking.stage.judit.io/tracking)
    .header("api-key", "c194e4c9-af6c-482f-b695-a1267c7d1ee7")
    .body("{"reference_type\": \"lawsuit_id\",\"reference_id\": \"0001234-55.2023.8.26.0100\",\"recurrence\": 2,\"tags\":\{\"category\": \"any\"}}")
    .asString();
    ```
</RequestExample>

<ResponseExample>
    ```json Response
    {
        "tracking_id": "754f02d7-e346-48e2-a23e-4455a11af41e",
        "user_id": "454bbd51-3f8a-4889-b45f-54a78e312257",
        "reference_type": "lawsuit_id",
        "reference_id": "0001234-55.2023.8.26.0100",
        "status": "created",
        "recurrence": 1,
        "created_at": "2023-10-17T15:17:05.826Z",
        "updated_at": "2023-10-17T15:17:05.826Z"
    }
    ```
</ResponseExample>
