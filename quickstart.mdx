---
title: 'Primeiros Passos'
---

Bem-vindo ao guia de primeiros passos da JUDIT! Vamos guiá-lo através dos passos necessários para começar a usar nossa API.

## Adquira Seu Plano

[Entre em contato para adquirir seu plano via WhatsApp](https://api.whatsapp.com/send/?phone=5521976748865)


## Gere Sua API Key

Entre em contato com o nosso time comercial para gerar facilmente uma API Key e começar a realizar seus testes, utilizando nossos serviços de forma simples e rápida.

## Usando a API Key

Com a API Key gerada, agora você pode começar a fazer solicitações para a API da JUDIT. Para isso, você deverá incluir a API Key no parâmetro `api-key` do cabeçalho das suas requisições.

## Buscando Seu Primeiro Processo

Para buscar um processo, você deverá fazer uma requisição POST para a rota `/requests`.

Aqui está um exemplo de como fazer isso usando curl:

```bash
curl -X POST "https://requests.prod.judit.io/requests" \
-H "api-key: YOUR_API_KEY" \
-H "Content-Type: application/json" \
-d '{
	"search": {
		"search_type": "lawsuit_cnj",
		"search_key": "0809570-97.2022.8.19.0002"
	}
}'
```


## Consumo da API

A JUDIT não assume responsabilidade por uso indevido da API. As cobranças serão aplicadas independentemente de erros ou desvios do padrão de formatação nos números de processo ou documentos enviados. Nas seções seguintes, apresentaremos detalhadamente cada campo e o formato aceito em nossa API.


